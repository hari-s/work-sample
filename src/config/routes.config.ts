import { environment } from './environment.config';

export const POST_ROUTE_AUTH = `${environment.simulatorBaseUrl}/auth`;
export const GET_ROUTE_RESULTS = `${environment.simulatorBaseUrl}/results`;
